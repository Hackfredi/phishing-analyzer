When ever the repository would be cloned create two .env files in two different folders

create first .env file inside the content folder and from the add.txt file copy all data present in file to the and replace xxxxx with api key generanted from virustotal.

create second .env file inside the fetch folder and from the add.txt file copy all data present in file to the and read the create data file present in fetch folder.
